# Site-wide QA audit — 2026-07-28

A full-site QA sweep for orphans, broken links, dead references to removed
features, stale copy that no longer matches the codebase, cross-page
terminology drift, and leftover scaffolding.

**When written, nothing in this document had been fixed.** It is a findings
record, written to be triaged item by item. Every finding was verified directly
against the files at the commit this document was added on; none is inferred or
taken on trust.

**Read the RESOLUTION LOG at the end before acting on anything here.** Roughly
half of Part 1 was worked later the same day, and the findings above are left
exactly as recorded rather than edited in place, so a section can describe a
problem that no longer exists.

Part 2 at the end captures the pending Medicare Advantage Insight Engine
description change that motivated the sweep, so the next session has the target
location and constraints without re-deriving them.

State when this was written: no site files modified, working tree clean.

---

# PART 1 — QA FINDINGS

## Genuinely broken

### `llms.txt:20` — live 404
Links `/skillsprout/`, deleted 2026-05-19. The homepage was updated to point at
the GitHub repo (`index.html:2624`); `llms.txt` was missed. No linter reads
`llms.txt`. Same file also omits `/colophon/`, `/case-study-care-redesign/`,
`/cv.html`, `/resume.html`, and the Medicare Advantage Insight Engine.

### Blog cross-post fragment links — 8 dead anchors
`blog/paper-saver-cloud-maturity-and-lock-in/index.html` links 8 times into
`/blog/paper-saver-estimating-xgboost-compute-costs-on-aws/#<heading-slug>`
(6 unique fragments). **No built post emits heading ids** — verified across all
244 posts, the target post contains exactly one id (`id="main"`).
`scripts/build_blog.py` has no heading-anchor step. Every link lands at the top
of a long post. Fix is either heading-anchor generation in the blog build (makes
all 8 work) or dropping the fragments.

Everything else resolves: all internal links in chrome pages and all 244 blog
posts check clean. The only unresolvable path is
`/medicare-advantage-insight-engine/`, which is expected (separate repo).

---

## Stale copy contradicting the current codebase

### `colophon/index.html` — public page, wrong on its own subject
- "**eleven** linters" at L7 (meta description), L13 (og:description), L21
  (twitter:description), L110 (SVG aria-label), L184 (body prose), and **L144
  renders "11 linters" as visible text in the figure**. Actual: twelve
  (`lint_ideas.py` added 2026-07-25 is missing from the spec list too).
  README and CLAUDE.md both correctly say twelve.
- "five Python pipelines" — defensible for `build_*.py` that generate pages, but
  `docs/pipelines.md` documents eleven.

### `TODO.md` — two entries describe finished or relocated work
- L79-83: "Custom 404.html … Currently GitHub Pages serves its generic 404."
  **Done.** `404.html` exists, styled, with the GoatCounter 404-prefix callback.
- L20-23: says the Bing `REPLACE_WITH_TOKEN` placeholder lives in `index.html`
  (~line 12) and `base.html`. It was removed from `index.html`; it survives only
  in `scripts/templates/blog/base.html:14-17`, commented out — and therefore
  ships (inert) into ~73 built blog pages.

### `index.html` — comments contradicting the code
- **L2232-2247**: comment block headed `EXPERIENCE - HEALTH CATALYST ENTRY`
  asserts "the expanded detail is NOT expand-collapsed. It is pulled inline."
  The entry *does* use `<details class="fold">` (L2350). The block also now sits
  above the `#work` anchor and the **BHA** entry, not Health Catalyst.
- **L2330**: "renders petrol teal via the palette selectors." Palette moved to
  Lichen moss green on 2026-07-23.
- **L524, L942**: both reference `.sankey-figure` as a live sizing peer.

### Other stale palette comments
- `case-study-care-redesign/index.html:62` — "renders petrol teal"
- `scripts/templates/resume/resume.html:25` — "oxford-red --accent token"
- `scripts/review/prompts/design-council.md:209` — "Tufte cream paper / cool
  neutral slate / petrol-teal". This one actively misleads future agent runs.

---

## Dead code / scaffolding

### `index.html:913-936` — dead `.sankey-figure` CSS
Three rules plus a section comment titled "SANKEY-STYLE TRANSITION FIGURE / The
SkillSprout worked example." No `class="sankey-figure"` exists in the file. The
figure was dropped when SkillSprout was demoted to a tile on 2026-07-01.
Verified by diffing every `.class` selector in the style block against every
`class=` attribute: this is the **only** dead class family.

### Numbering collision
The inline stylesheet has two sections numbered **19** — `19. PRINT OVERRIDES`
(L1576) and `19. SPLIT HERO` (L1626). CLAUDE.md's "sections 19 / 20 / 20.1"
reference is ambiguous as a result.

### Unreferenced files
- `scripts/figures/credentialing_sim.py`, `scripts/figures/tournament_sim.py` —
  referenced by no workflow, doc, test, or post.
- `docs/critique/subpage-backlog.md` — referenced nowhere; all four of its source
  citations point into `archive/design-review-presentations/`, which does not
  exist.
- `.impeccable/` — one critique artifact, not in the README tree, not gitignored.

### Internal artifacts publicly fetchable
`robots.txt` disallows `/src/`, `/scripts/`, `/archive/` but not `/critiques/`,
`/evaluations/` (contains a hiring eval), `/reviews/`, `/docs/`,
`/data/snapshots/`, `TODO.md`, `CLAUDE.md`, `README.md`.

---

## Cross-surface factual contradictions

### Contact email — highest real-world cost
`resume.md:3` / `resume.html:193` advertise **`zaherkarp@gmail.com`**.
`cv.md:3`, `cv.html:205`, `index.html:3443`, and CLAUDE.md all use
`me@zaherkarp.com`. The resume is the surface most likely to reach a recruiter.

### The `$50M` cliff number has four denominators
| Surface | Claim |
|---|---|
| `index.html:2475`, `:2510` | $50M **for a mid-size contract** |
| `star-rating-predictor/index.html:446` | **$50–80M/yr for a 200,000-member plan** |
| `star-rating-predictor/index.html:705` (JS tooltip) | **~$50M+ for large plans** |
| `star-rating-predictor/index.html:7`, `:13` (meta/og) | **$50M+**, no denominator |
| `star-rating-predictor-methodology.md:6` | $50M **to a mid-size contract** |
| `star-rating-predictor-methodology.md:12` | **$50M+ annually for large contracts** |

Lines 6 and 12 of the same post disagree; the subpage's hero disagrees with its
own tooltip and meta tag. CLAUDE.md §Calibrated claims requires naming the
denominator behind any ratio.

### healthfinch outcome numbers disagree
- `index.html:2394` — "roughly ten users to more than one hundred" (~10×);
  "about 400 hours of **annual** manual reporting"; "over $1M in recurring revenue"
- `resume.md:33` — "**7×** user growth"; "400+ prep hours **per quarter**";
  "$1M+ in **new** recurring revenue"

Both the multiple and the period conflict. The bare "7×" is also exactly the
construction CLAUDE.md §Calibrated claims forbids.

### Counts that disagree with the CV
- `index.html:2422` "advancing through **five** positions" vs `cv.md` Past
  Research Positions listing **six**.
- `resume.md:39` "**7** publications and conference proceedings" vs 6 entries in
  `publications.yaml` and "6 publications" in both dot-plot aria-labels.
- `resume.md:40` "compliance manager on **six** grants" vs 5 of 7 CV grants
  naming that role.

Verified consistent (no action): 17 presentations reconciles exactly against the
CV (9 presentations + 8 posters), including per-year counts; 6 publications;
"24 weeks / 34 posts"; 4,680 weeks.

### Sustainable Clarity is homepage-only
`index.html:2443` carries a full 2007–2014 Principal role, plus a testimonial and
a career-arc band. It appears in neither `resume.md` nor `cv.md` — and the CV is
the surface `lint_facts.py` calls comprehensive.

---

## Terminology inconsistencies

### "healthfinch" vs "Healthfinch"
CLAUDE.md §Vocabulary declares lowercase. Clean split by surface, caught by no
linter: lowercase 10× in `index.html`; capitalized in `resume.md:26`,
`cv.md:34`, `resume.html:209`, `cv.html:232`, six lines of `skills.yaml`, and
`practice-automation-workflow-roi.md:3,10`.

### Job title, third rendering
`index.html:2166` (About margin note) says "Manager **of** Data Science **&**
Engineering"; the h3, resume, CV, and JSON-LD all say "Manager, Data Science
**and** Engineering". `lint_facts` folds "of"↔"," but only parses `<h3>` +
`<p class="meta">`, so the margin note is invisible to it.

Same line: the margin note says BHA is in **Madison WI**; `index.html:2258` and
`resume.md:11` say **remote**.

### Degree suffix, three forms
"Zaher Karp, **M.P.H.**" (resume) / "Zaher Karp, **MPH**" (CV) / "Master of
Public Health (MPH)" (homepage).

### Past-role titles, four renderings
`cv.md:34` "Analytics Manager" / `resume.md:17,26` "Healthcare Analytics
Manager" / `index.html:2316,2371` "Healthcare Analytics Manager, Embedded
Refills and Care Gaps" and "…, Specialist". `cv.md` also merges healthfinch +
Health Catalyst into one 2017–2025 appointment where the other two split at the
2020 acquisition.

### Presentation date range disagrees with itself
`index.html:3024` "between 2010 and **2017**" vs `:2679` figcaption and both
aria-labels (`:2682`, `:2821`) "2010 to **2019**". 2019 is the publications-lane
axis end. Same figure labels the lane "presentations" on desktop (`:2687`) and
"talks" on mobile (`:2826`).

### SkillSprout is audible but not visible
`index.html:2619` titles the card "Healthcare Workforce Transition Platform";
"SkillSprout" appears only in `aria-label` and `data-goatcounter-title` at
`:2624`. Screen-reader users hear a product name sighted users never see.

---

## Navigation and metadata

### Blog and colophon still ship the retired nine-item nav
The homepage nav is four lowercase items (`writing / work / about / contact`).
`blog/index.html:41`, `blog/archive/index.html:41`, and `colophon/index.html:79`
carry the old nine Title-Case items (About / Writing / Experience / Projects /
Publications / Speaking / Education / Service / Contact). Source of the blog
variant: `scripts/templates/blog/base.html:42`; the colophon drifted by hand.

Label collision: `/blog/` is "Writing" in those navs and "Blog" in every footer,
while the homepage nav's "writing" points at the in-page `#writing` anchor —
which since Direction B holds **only the cadence sparkline**, since the actual
recent-writing list moved up into the hero.

### Em dashes in blog page titles — policy violation
`scripts/build_blog.py` hardcodes an em dash in every blog page title:
L285 `f"{post['title']} — Zaher Karp"`, L337, L355, L408, L423. That string
feeds `<title>`, `og:title`, and `twitter:title` on ~250 pages. Site chrome is
supposed to be em-dash-clean; the pre-push grep only covers `index.html`,
`resume.md`, `cv.md`, and `life-in-weeks/index.html`, so `blog/` output is
unguarded.

### Title conventions, four variants
name-first comma (`index.html`) / name-last comma (subpages, 404, colophon) /
name-last **em dash** (blog, archive, tags) / name-last **pipe**
(`epidemic-simulation/index.html:6` — the only pipe on the site).

### Missing OG / Twitter tags
`life-in-weeks/`, `epidemic-simulation/`, `resume.html`, `cv.html` all lack
`og:site_name` and every `twitter:*` tag. `404.html` has no OG tags and no
canonical (defensible — it is `noindex`).
`og:type` splits three ways: `website` / `profile` (resume, cv) / **`article`**
(`epidemic-simulation/index.html:15`, the only interactive subpage typed so).

### GoatCounter script src is inconsistent
The form documented in CLAUDE.md (`//gc.zgo.at/count.js`) is the minority: 3
pages use it (404, resume, cv), 6 use `https://gc.zgo.at/count.js`. Functionally
equivalent; only the doc is wrong about which is canonical.

### `404.html:67` — footer missing the Colophon link
Four links where every other footer has five.

---

## Reachability gaps (not broken, but weak)

- **`/epidemic-simulation/` is effectively orphaned.** `index.html` contains zero
  occurrences of `epidemic-simulation`, `two-states`, `pathogen`, or `SEIRV`. It
  is reachable only via `/blog/` → the two-states post → the simulator (3 hops),
  plus `sitemap.xml` and `llms.txt`. **CLAUDE.md L956-958 justifies excluding it
  from the blog Experiments list by claiming it has a first-class project card or
  writing-section entry on the homepage — that is false.** Either the page needs
  a homepage entry, or it should join `/life-in-weeks/` in `EXPERIMENTS`, or
  CLAUDE.md's rationale needs correcting.
- **`cv.html` / `cv.pdf`** reachable only from `index.html:3450`. **Not in
  `sitemap.xml`** — `write_sitemap` in `build_blog.py:465` hardcodes
  `resume.html` with no CV counterpart. `cv.html:199` links to `resume.html`;
  `resume.html` does not link back.
- `blog/tags/index.html` reachable only from `blog/index.html:62`.
- `#education`, `#service`, `#certifications`, `#testimonials` have no inbound
  link from the homepage nav or the `.hero-more` line (blog-post navs do reach
  `#education` and `#service`).

## Documentation drift (repo-internal)

- `README.md:526` "the **nine** linters" — contradicts its own L123 and L220
  ("twelve").
- `README.md:686` "All **7** `<details>` folds (4 experience + speaking + 2
  testimonials)" — actual is **11** (4 experience, projects-index, speaking,
  education, service, **3** testimonials).
- `README.md:690` "SkillSprout slope graph renders inside the … project body" —
  removed on demotion; the twin of the dead `.sankey-figure` CSS.
- `README.md:692` "Gantt renders between Testimonials and Education" — Direction
  B moved Testimonials to the end; the Gantt now sits between Speaking and
  Education.
- `README.md` trees omit `404.html`, `llms.txt`, `cv.*`, `TODO.md`,
  `epidemic-simulation/`, `colophon/`, `case-study-care-redesign/`,
  `build_palette.py`, `scripts/tests/`, and 6 of 11 workflows.
  `.github/workflows/lighthouse.yml` appears in neither README nor CLAUDE.md.
- `CLAUDE.md:496-499` still names `tl-compact` as one of the two clickable-band
  SVGs; it was retired 2026-07-26 (the pair is `tl-horizontal` / `tl-rail`).
- `CLAUDE.md:745` references the dead `.sankey-figure` sizing idiom.
- `CLAUDE.md:136` "inline CSS (~2,570 lines)" — the `<style>` block is L63-1802,
  about 1,740.
- `docs/pipelines.md:192` "**Nine** pipelines … Five wired to CI" — the document
  then enumerates **eleven**, and omits the palette pipeline entirely.

## Third-party asset risk
`blog/weighting-provider-panels/index.html:83` is the only `<img>` on the site
and hotlinks a Squarespace CDN URL. Link-rot dependency, invisible to every
current check.

## Verified clean
Em-dash count in `index.html` = 0. Accent occurrences = 16 (cap 20).
GoatCounter present on every page. All sitemap `<loc>` entries resolve. No
Astro/Tailwind/EB Garamond/Google Fonts/640px/stats-table/now.yaml/`teaser-row`/
`tl-compact`/reduced-motion remnants in any served file. All canonical and OG
URLs use `zaherkarp.com` — no `github.io` leakage. No TODO/FIXME markers in
production HTML/CSS. Working tree clean.

---

# PART 2 — Medicare Advantage Insight Engine description (pending)

Not started. Recorded here so the next session inherits the target and the
constraints. The requested change: state a three-tier simulation estimate and
the early production data as two clearly separate claims, never blended, with
both caveats intact (the simulation is a reasoned model with assumed
parameters, not measured data; the production numbers do not yet reflect a
stable baseline). Match the page's existing tone and formatting rather than
introducing a new structure.

## Where it lives

`index.html:1955-1983`, inside `.split-hero` → `.hero-project` → `.project`
(featured project 01 by CSS counter):

- **L1957** prose: "A Stars analyst skims about 200 healthcare news items a week;
  roughly five of them matter. This engine codifies that triage. It scores each
  item on six named features (…) and posts the ones that clear the threshold to
  a Teams webhook, each with a per-feature breakdown for explainability."
- **L1972-1983** `figure.funnel-figure`: a two-tier SVG, `~200 news items / week`
  → `~5 that matter`, with an aria-label repeating both numbers, plus a
  figcaption.

Adjacent surfaces stating the same uncaveated claim (out of scope unless asked):
`src/content/blog/medicare-advantage-insight-engine.md:10`,
`docs/homepage-iteration-2026-07-26.md:107`, `CLAUDE.md`.

## Constraints on the edit

- Em-dash clean (`grep -c '—' index.html` must stay 0).
- Figure numbers must match the prose exactly (existing figure convention).
- Funnel figure is accent-free by design; keep it so.
- CLAUDE.md §Calibrated claims: scope every claim, name the denominator.
- The figure currently encodes two tiers; the new prose describes three. Either
  add the middle relevance tier to the SVG or keep the figure as the outer
  bracket only — an explicit decision for the user.

## Verification

- `python scripts/lint_html.py && python scripts/lint_links.py && python
  scripts/lint_vocab.py && python scripts/lint_notes.py && python
  scripts/lint_markers.py`
- `grep -c '—' index.html` → 0; `grep -cE -- '--accent|#7a0000' index.html` ≤ 20
- Visual check of the hero project card at desktop and ≤760px.

---

# RESOLUTION LOG

Appended 2026-07-28. Findings above are left exactly as recorded; this section
tracks what has since been actioned. Everything not listed here is still open.

## Fixed

- **`llms.txt:20`** — now points at `github.com/zaherkarp/skillsprout` with a
  pointer to the write-up, mirroring the homepage tile. `/skillsprout/` no
  longer appears in any served file.
- **Blog cross-post fragment links** — fixed at the root rather than per link:
  `scripts/build_blog.py` now applies `mdit-py-plugins`' `anchors_plugin` at
  h2-h3, so every built post emits heading ids. All 6 unique fragments resolve
  exactly under the stock slugifier; no duplicate ids in any post; nothing
  collides with the template's `id="main"`. The 8 hrefs were left as written.
  Built output is regenerated by `build_blog.yml` on merge to `main`.
- **Part 2, the description** — landed. The open question at L334-336 was
  decided in favour of a third band: the figure is now three-tier
  (`~200 news items / week` ▸ `~20 screened` ▸ `~5 that matter`), with the
  simulation caveat carried in the figcaption so the numbers never travel
  without it. Prose, aria-label, figcaption, and both in-file comments were
  updated together.

  **Amended later the same day.** That rewrite left the description at 129 words
  in the ~38% hero column, and `.split-hero`'s `align-items: start` rendered the
  overhang as dead grid space: 488px under the writing column at 1440px, 822px at
  900px. The prose is now ~62 words (84px / 155px measured after), with the
  six-feature list, the assumed-parameters elaboration, and the early-production
  reading relocated to the methodology post. All three claims survive; only their
  location changed. The constraint learned: the Part 2 checklist at L328-336
  should have carried a **length budget** for that column alongside its accuracy
  constraints, since the two pull against each other.

## Fixed later the same day (2026-07-28, PR #113)

A second pass worked the audit as a checklist. These findings above are now
closed; the "everything not listed here is still open" line at the top of this
log predates them.

- **`colophon/index.html`, "eleven linters"** — all six references corrected to
  twelve, including the visible SVG text, and the missing twelfth
  (`lint_ideas.py`) added to the list the page names. The page had been
  describing eleven *while naming eleven*, so both halves were wrong together.
- **Dead `.sankey-figure` CSS** — the block and its two stale in-file references
  are removed.
- **Section-number collision** — the split-hero family renumbered to 20 / 21 /
  21.1, so `19. PRINT OVERRIDES` is unambiguous. CLAUDE.md's cross-reference
  updated with it.
- **`robots.txt`** — `/critiques/`, `/evaluations/`, `/reviews/`, `/docs/`,
  `/data/`, `TODO.md`, `CLAUDE.md`, `README.md` now disallowed for general
  crawlers, with a comment recording why. The AI-crawler blocks were already
  `Disallow: /`.
- **Contact email** — `resume.md` now uses `me@zaherkarp.com`, matching the CV,
  homepage, JSON-LD, and CLAUDE.md.
- **The `$50M` denominators** — the two scoped claims are both true at different
  scales and were kept; the three unscoped ones were fixed (the simulator's
  `meta`, `og:description`, and JS tooltip), as was the methodology post's
  L12-vs-L6 self-contradiction.
- **healthfinch outcome numbers** — the resume is authoritative (7x, per
  quarter, *new* recurring). Reconciling forced a figure change, since the
  drawn endpoints (~10 to 100+) are a *tenfold* multiple that no caption edit
  can square with 7x; the figure now encodes the ratio instead (1:7 bar widths,
  `baseline` / `7x baseline`). The `$1M` was also mis-attributed and moved to
  the ROI work.
- **Em dashes in blog page titles** — fixed at the source in `build_blog.py`
  and regenerated. The separator is a comma, which also collapses two of the
  four title variants this audit flagged. An em dash in the archive page's
  *visible intro prose*, which this audit missed, was fixed with it.
- **Documentation drift** — `README.md` (linter count, `<details>` fold count,
  the removed SkillSprout slope graph, the Gantt's position), `CLAUDE.md`
  (inline-CSS line count, the retired `tl-compact`, the dead `.sankey-figure`
  idiom, the "Featured" label contradiction), and `docs/pipelines.md`
  (nine -> eleven, with the CI/manual/external split counted rather than
  estimated).
- **Stale palette comments** — `index.html` (two, including the `:119` token
  block header still describing Tufte cream), `case-study-care-redesign/`,
  `scripts/templates/resume/resume.html` ("oxford-red"), and
  `scripts/review/prompts/design-council.md`. The last one mattered most: it is
  the Design Council's own briefing, so it was actively misleading every panel
  run, and the panels are now always convened.
- **`index.html` Health Catalyst comment** — it asserted the expanded detail is
  "NOT expand-collapsed" while the entry uses `<details class="fold">`. Rewritten
  to describe what actually ships.

**Missing OG / Twitter tags, closed 2026-07-28.** `og:site_name` and the four
`twitter:*` tags were added to `/life-in-weeks/` and `/epidemic-simulation/`
directly, and to the resume and CV **web templates** (not their generated output,
which CI rebuilds). `/epidemic-simulation/`'s `og:type` was `article`, the only
interactive subpage typed so, and is now `website` like its peers. `og:type`
stays `profile` on the resume and CV: those are a person's documents, and that is
what the type is for, so it was never drift. `404.html` keeps no OG tags, which
this audit already called defensible for a `noindex` page. Social titles are now
paired everywhere: `og:title` may differ from `<title>` (the reference pages
already do this, using a more descriptive share title), but `og:title` and
`twitter:title` always match each other.

**Title conventions, closed 2026-07-28.** The audit counted four variants; there
are really two, and only one was drift. `/epidemic-simulation/` carried the
site's only pipe and is now `Two states, one pathogen, Zaher Karp` (trimmed
rather than comma-joined, since the full title already holds a comma and a colon
and a fourth clause read as a run-on). The homepage's name-first form is
**deliberate and stays**: the homepage is the person, every other page is a topic
within the site, and name-first fronts the search term on the one page that
should rank for it. Counting it as drift was the miscount. The rule is now
written down in CLAUDE.md §Page title convention so it is not re-flagged.

## Noted while fixing, still open

- The audit listed 5 surfaces for the description; there are 7. It missed the
  built HTML at `blog/medicare-advantage-insight-engine/index.html:88,147`
  (regenerated by CI) and the two in-file comments at `index.html:965-971` and
  `:1968`, both of which restated the numbers. All are now consistent.
- `CLAUDE.md:778-783` says both featured projects sit inside
  `<section id="projects">`; the Medicare card moved into `.hero-project` in the
  2026-07-26 iteration. Only the funnel numbers on that line were corrected.
- The methodology post grounds its volume figures in direct observation
  ("The first time I sat with an analyst doing this triage"), which sits
  awkwardly beside calling them a model with assumed parameters. L10 threads it;
  the observational framing at L14 was left alone.
- `lint_links.py:63` stops path capture at `#` by design, so cross-page
  fragments are never validated. This class of dead link can recur silently.
- Rebuilding `blog/` also rewrites JSON-LD `dateModified` from `...Z` to
  `...+00:00` on 9 posts, pre-existing drift between the committed output and
  what the current build produces. Unrelated to the anchors change.
